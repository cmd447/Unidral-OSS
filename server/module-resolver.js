"use strict";

const modulesStore = require("./modules-store");

function resolveModuleConfig(site, engineSettings) {
  return site;
}

module.exports = {
  resolveModuleConfig: resolveModuleConfig
};
